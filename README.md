### Splendor Protocol

Double-dipping stablecoin yield since 2022


## Testing
`python3 get_current_slot.py && anchor test` a few times, will fail to find the right slot a few times probabilistically
